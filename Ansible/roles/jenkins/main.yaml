- name: Add Jenkins repo key
  apt_key:
    url: https://pkg.jenkins.io/debian-stable/jenkins.io.key
    state: present- name: Add Jenkins repo
  apt_repository:
    repo: 'deb https://pkg.jenkins.io/debian-stable binary/'- name: Install Jenkins
  apt:
    name: jenkins
    state: present- name: Ensure Jenkins running
  service:
    name: jenkins
    state: started
    enabled: true
